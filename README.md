# Bn2flow
Bangla code to flowchart and run.

---

# 📘 ১ - ৫০টি পূর্ণাঙ্গ কোড উদাহরণ

---

# 🔹 বেসিক আউটপুট ও চলক (১-১০)

### ১. হ্যালো ওয়ার্ল্ড
```
দেখাও("হ্যালো পৃথিবী!");
```

### ২. চলক ঘোষণা
```
চলক নাম = "সফটওয়্যার";
দেখাও(নাম);
```

### ৩. ধ্রুবক ব্যবহার
```
ধ্রুবক পাই = ৩.১৪;
দেখাও(পাই);
```

### ৪. যোগফল
```
চলক ক = ১০;
চলক খ = ৫;
দেখাও(ক + খ);
```

### ৫. বিয়োগফল
```
চলক ক = ২০;
চলক খ = ৮;
দেখাও(ক - খ);
```

### ৬. গুণফল
```
চলক ক = ৫;
চলক খ = ৪;
দেখাও(ক * খ);
```

### ৭. ভাগফল
```
চলক ক = ১০০;
চলক খ = ১০;
দেখাও(ক / খ);
```

### ৮. ভাগশেষ (Modulo)
```
দেখাও(১০ % ৩);
```

### ৯. বাংলা সংখ্যা ক্যালকুলেশন
```
চলক x = ১০;
দেখাও(x + ৫);
```

### ১০. স্ট্রিং কনক্যাটিনেশন
```
দেখাও("শুভ" + " " + "সকাল");
```

---

# 🔹 কন্ডিশনাল লজিক (১১-২০)

### ১১. যদি (If)
```
যদি (১০ > ৫) {
দেখাও("সত্য");
}
```

### ১২. যদি-নাহলে
```
যদি (৫ > ১০) {
দেখাও("হ্যাঁ");
}
নাহলে {
দেখাও("না");
}
```

### ১৩. জোড়-বিজোড়
```
চলক n = ৭;

যদি (n % ২ == ০) {
দেখাও("জোড়");
}
নাহলে {
দেখাও("বিজোড়");
}
```

### ১৪. পাস-ফেল
```
চলক m = ৪০;

যদি (m >= ৩৩) {
দেখাও("পাস");
}
নাহলে {
দেখাও("ফেল");
}
```

### ১৫. বড় সংখ্যা নির্ণয়
```
চলক a = ১০;
চলক b = ২০;

যদি (a > b) {
দেখাও(a);
}
নাহলে {
দেখাও(b);
}
```

### ১৬. নেস্টেড কন্ডিশন
```
যদি (১০ > ৫) {
    যদি (৫ > ২) {
        দেখাও("সঠিক");
    }
}
```

### ১৭. এবং (AND)
```
যদি (১০ > ৫ এবং ৫ > ২) {
দেখাও("উভয় সত্য");
}
```

### ১৮. অথবা (OR)
```
যদি (১০ < ৫ অথবা ৫ > ২) {
দেখাও("একটি সত্য");
}
```

### ১৯. শূন্য যাচাই
```
চলক n = ০;

যদি (n == ০) {
দেখাও("শূন্য");
}
```

### ২০. ধনাত্মক-ঋণাত্মক
```
চলক n = -৫;

যদি (n > ০) {
দেখাও("ধনাত্মক");
}
নাহলে {
দেখাও("ঋণাত্মক");
}
```

---

# 🔹 লুপ বা পুনরাবৃত্তি (২১-৩০)

### ২১. ১-৫ গণনা
```
চলক i = ১;

যতক্ষণ (i <= ৫) {
দেখাও(i);
i = i + ১;
}
```

### ২২. ৫-১ উল্টো গণনা
```
চলক i = ৫;

যতক্ষণ (i >= ১) {
দেখাও(i);
i = i - ১;
}
```

### ২৩. নামতা (২ এর নামতা)
```
চলক i = ১;

যতক্ষণ (i <= ১০) {
দেখাও(২ * i);
i = i + ১;
}
```

### ২৪. জোড় সংখ্যা
```
চলক i = ২;

যতক্ষণ (i <= ১০) {
দেখাও(i);
i = i + ২;
}
```

### ২৫. বিজোড় সংখ্যা
```
চলক i = ১;

যতক্ষণ (i <= ১০) {
দেখাও(i);
i = i + ২;
}
```

### ২৬. ১-১০ এর যোগফল
```
চলক sum = ০;
চলক i = ১;

যতক্ষণ (i <= ১০) {
sum = sum + i;
i = i + ১;
}

দেখাও(sum);
```

### ২৭. ব্রেকের উদাহরণ
```
চলক i = ১;

যতক্ষণ (i <= ১০) {

যদি (i == ৫) {
i = ১১;
}

দেখাও(i);
i = i + ১;

}
```

### ২৮. স্কয়ার নাম্বার
```
চলক i = ১;

যতক্ষণ (i <= ৫) {
দেখাও(i * i);
i = i + ১;
}
```

### ২৯. লুপ সীমা
```
চলক i = ০;

যতক্ষণ (i < ৩) {
দেখাও("চলছে");
i = i + ১;
}
```

### ৩০. মাল্টিপল কন্ডিশন লুপ
```
চলক i = ১;

যতক্ষণ (i < ১০ এবং i != ৫) {
দেখাও(i);
i = i + ১;
}
```

---

# 🔹 অ্যারে ও স্ট্রিং (৩১-৪০)

### ৩১. অ্যারে তৈরি
```
চলক ফল = ["আম","জাম"];
দেখাও(ফল);
```

### ৩২. অ্যারে ইনডেক্স
```
চলক ডাটা = [১০,২০];
দেখাও(ডাটা[১]);
```

### ৩৩. অ্যারে লেংথ
```
চলক x = [১,২,৩];
দেখাও(x.length);
```

### ৩৪. অ্যারে পুশ
```
চলক x = [১];
x.push(২);
দেখাও(x);
```

### ৩৫. স্ট্রিং লেংথ
```
চলক s = "বাংলা";
দেখাও(s.length);
```

### ৩৬. স্ট্রিং স্লাইস
```
চলক s = "বাংলাদেশ";
দেখাও(s.slice(০,৪));
```

### ৩৭. আপারকেস
```
চলক s = "hello";
দেখাও(s.toUpperCase());
```

### ৩৮. অ্যারে লুপ
```
চলক a = [১,২,৩];
চলক i = ০;

যতক্ষণ (i < a.length) {
দেখাও(a[i]);
i = i + ১;
}
```

### ৩৯. স্ট্রিং ইনডেক্স
```
চলক s = "কোড";
দেখাও(s[০]);
```

### ৪০. অ্যারে পপ
```
চলক a = [১,২];
a.pop();
দেখাও(a);
```

---

# 🔹 ফাংশন ও রিকার্সন (৪১-৫০)

### ৪১. সাধারণ ফাংশন
```
ফাংশন হাই() {
দেখাও("হাই");
}

হাই();
```

### ৪২. প্যারামিটার ফাংশন
```
ফাংশন যোগ(ক, খ) {
ফেরত ক + খ;
}

দেখাও(যোগ(৫,৫));
```

### ৪৩. বর্গ ফাংশন
```
ফাংশন বর্গ(n) {
ফেরত n * n;
}

দেখাও(বর্গ(৪));
```

### ৪৪. ফ্যাক্টোরিয়াল
```
ফাংশন f(n) {

যদি (n <= ১) {
ফেরত ১;
}

ফেরত n * f(n-১);

}

দেখাও(f(৫));
```

### ৪৫. ফিবোনাচি
```
ফাংশন fib(n) {

যদি (n <= ১) {
ফেরত n;
}

ফেরত fib(n-১) + fib(n-২);

}

দেখাও(fib(৬));
```

### ৪৬. কাউন্টডাউন
```
ফাংশন c(n) {

যদি (n > ০) {
দেখাও(n);
c(n-১);
}

}

c(৩);
```

### ৪৭. ডিজিট সাম
```
ফাংশন s(n) {

যদি (n == ০) {
ফেরত ০;
}

ফেরত (n % ১০) + s(Math.floor(n/১০));

}

দেখাও(s(১২৩));
```

### ৪৮. বুলিয়ান রিটার্ন
```
ফাংশন সত্য_কি() {
ফেরত সত্য;
}

দেখাও(সত্য_কি());
```

### ৪৯. স্ট্রিং রিভার্স
```
ফাংশন rev(s) {

যদি (s == "") {
ফেরত "";
}

ফেরত rev(s.substr(১)) + s[০];

}

দেখাও(rev("abc"));
```

### ৫০. পাওয়ার ফাংশন
```
ফাংশন p(b, e) {

যদি (e == ০) {
ফেরত ১;
}

ফেরত b * p(b, e-১);

}

দেখাও(p(২,৩));
```

---

# 🎯 লক্ষ্য

এই উদাহরণগুলো ব্যবহার করে আপনি Bn2Flow IDE-তে

- বাংলা কোড লিখতে পারবেন  
- JavaScript কম্পাইল দেখতে পারবেন  
- Flowchart জেনারেট করতে পারবেন  

---

⭐ Happy Coding in Bangla!
