# Bangla2flow
> Bangla code to flowchart and run.

### সিন্টেক্স
```py
দেখাও("ওহে বিশ্ব");
```

### চলক ঘোষণা
```py
চলক নাম = "আনিছুর রহমান";
দেখাও(নাম);
```

### ইনপুট ও আউটপুট
```py
চলক নাম = নাও("তোমার নাম লিখো: ");
দেখাও(নাম);
```


### ধ্রুবক ব্যবহার
```py
ধ্রুবক পাই = ৩.১৪;
দেখাও(পাই);
```

### যোগফল
```py
চলক ক = ১০;
চলক খ = ৫;
দেখাও(ক + খ);
```


### ভাগশেষ (Modulo)
```py
দেখাও(১০ % ৩);
```

### সংখ্যা ক্যালকুলেশন
```py
চলক ক = ১০;
দেখাও(ক + ৫);
```

### স্ট্রিং কনক্যাটিনেশন
```py
দেখাও("শুভ" + " " + "সকাল");
```

### যদি-নাহলে
```py
যদি (৫ > ১০) {
দেখাও("হ্যাঁ");
}
নাহলে {
দেখাও("না");
}
```

### জোড়-বিজোড়
```py
চলক n = ৭;

যদি (n % ২ == ০) {
দেখাও("জোড়");
}
নাহলে {
দেখাও("বিজোড়");
}
```

### বড় সংখ্যা নির্ণয়
```py
চলক a = ১০;
চলক b = ২০;

যদি (a > b) {
দেখাও(a);
}
নাহলে {
দেখাও(b);
}
```

###  এবং ও অথবা
```py
যদি (১০ > ৫ এবং ৫ > ২) {
দেখাও("উভয় সত্য");
}

যদি (১০ < ৫ অথবা ৫ > ২) {
দেখাও("একটি সত্য");
}
```

### স্কয়ার নাম্বার
```py
চলক ক = ১;

যতক্ষণ (ক <= ৫) {
দেখাও(ক * ক);
ক = ক + ১;
}
```
## অ্যারে
```py
চলক ফল = ["আম","জাম"];
দেখাও(ফল);
```

### অ্যারে ইনডেক্স
```py
চলক ডাটা = [১০,২০];
দেখাও(ডাটা[১]);
```

###  অ্যারে লেন্থ
```py
চলক ক = [১,২,৩];
দেখাও(ক.length);
```

### অ্যারে পুশ
```py
চলক ক = [১];
ক.push(২);
দেখাও(ক);
```

### স্ট্রিং লেন্থ
```py
চলক শব্দদ = "বাংলা";
দেখাও(শব্দ.length);
```

### স্ট্রিং স্লাইস
```py
চলক শব্দ = "বাংলাদেশ";
দেখাও(শব্দ.slice(০,৫));
```

### আপারকেস
```py
চলক শব্দ = "hello";
দেখাও(শব্দ.toUpperCase());
```

### অ্যারে লুপ
```py
চলক ক = [১০,২০,৩০,৪০];

চলক খ = ০;

যতক্ষণ (খ < ক.length) {
    দেখাও(ক[খ]);
    খ = খ + ১;
}
```

###  স্ট্রিং ইনডেক্স
```py
চলক শব্দ = "কোড";
দেখাও(s[০]);
```

### অ্যারে পপ
```py
চলক ক = [১,২];
ক.pop();
দেখাও(ক);
```



### প্যারামিটার ফাংশন
```py
ফাংশন যোগ(ক, খ) {
ফেরত ক + খ;
}

দেখাও(যোগ(৫,৫));
```

###  বর্গ ফাংশন
```py
ফাংশন বর্গ(ক) {
ফেরত ক * ক;
}

দেখাও(বর্গ(৪));
```

### ফ্যাক্টোরিয়াল
```py
ফাংশন ক(ন) {

যদি (ন <= ১) {
ফেরত ১;
}

ফেরত ন * ক(ন-১);

}

দেখাও(ক(৫));
```

###  ফিবোনাচি
```py
ফাংশন ফিব(ন) {

    যদি (ন <= ১) {
        ফেরত ন;
    }

    ফেরত ফিব(ন-১) + ফিব(ন-২);

}

চলক ক = ১;

যতক্ষণ (ক <= ১০) {
    দেখাও(ফিব(ক));
    ক = ক + ১;
}
```



###  বুলিয়ান 
```py
দেখাও(৫ > ৩);  
দেখাও(১০ == ১৫);
দেখাও("লিখন" == "লিখন"); 
দেখাও("বাংলা" != "কোড");
দেখাও((১০ > ৫) && (২০ < ৩০));
দেখাও((৫ > ১০) || (২০ < ৩০));
দেখাও(!(৫ > ১০));
দেখাও("লিখন".দৈর্ঘ্য > ০);
দেখাও(৭ % ২ == ০);
দেখাও(৮ % ২ == ০);
সঠিক = সত্য;
দেখাও(সঠিক);
```

### স্ট্রিং রিভার্স
```py
ফাংশন উল্টো(শব্দ) {

যদি (শব্দ == "") {
ফেরত "";
}

ফেরত উল্টো(শব্দ.substr(১)) + শব্দ[০];

}

দেখাও(উল্টো("আনিছুর"));
```

### পাওয়ার ফাংশন
```py
ফাংশন পাওয়ার(বেস, ঘাত) {

যদি (ঘাত == ০) {
ফেরত ১;
}

ফেরত বেস * পাওয়ার(বেস, ঘাত-১);

}

দেখাও(পাওয়ার(২,৩));
```



# 🎯 লক্ষ্য

এই উদাহরণগুলো ব্যবহার করে আপনি Bangla2Flow IDE-তে

- বাংলা কোড লিখতে পারবেন  
- JavaScript কম্পাইল দেখতে পারবেন  
- Flowchart জেনারেট করতে পারবেন  


⭐ Happy Coding in Bangla!
